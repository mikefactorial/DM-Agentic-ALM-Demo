# Strong Name Key

Place the plugin signing key at:

DynamicMinds.AgenticALMSample.snk

The plugin projects are configured to sign assemblies when this key exists.

Example generation command (developer machine with sn tool installed):

sn -k DynamicMinds.AgenticALMSample.snk
